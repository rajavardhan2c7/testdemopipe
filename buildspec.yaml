version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto11
  build:
    commands:
      - mvn package
      - sam package
        --template-file sam-template.yml
        --s3-bucket codepipeline-ap-south-1-794748969034Info
        --output-template-file packaged-template.yml
artifacts:
  files:
    - packaged-template.yml
